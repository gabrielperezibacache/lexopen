# LexOpen Desktop

Shell Electron para **Host** (servidor del estudio) y **Cliente** (URL Tailscale).

Documentación completa: [../docs/DESKTOP.md](../docs/DESKTOP.md)

```bash
# Desde la raíz del repo
npm install
npm --prefix desktop install
npm run desktop:test
npm run desktop:dev
```
